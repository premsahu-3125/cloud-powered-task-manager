# Base URL of the backend API.
# Local development (backend run directly with `npm run dev`): http://localhost:5000/api
# Local development with Docker Compose: http://localhost:5000/api (still the mapped host port)
VITE_API_URL=http://localhost:5000/api
